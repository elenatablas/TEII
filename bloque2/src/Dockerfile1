# 2 capas de nuestra imagen
FROM ubuntu

# Puede ir en cualquier nivel de la imagen, normalmente al inicio
LABEL version=1.0
LABEL description="Muestra la fecha actual por pantalla cada segundo." 
LABEL vendor=elenatablas

COPY fecha.sh /fecha.sh
ENTRYPOINT ["/fecha.sh"]

# Para saber las capas que tiene
# docker history -H fecha:latest



# docker logs -f sad_hermann 
# Iniciando container...

# Construir imagen a partir del Dockerfile
# docker build [-t|--tag] [nombre-imagen-resultante] .
# Ejemplo: docker build -t fecha .

